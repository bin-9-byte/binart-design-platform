收到，我们将按照**方案 2：订阅更新 (Subscribe)** 进行修改。

### 实施目标
将原本的搜索功能替换为“订阅”功能，在头部展示 Subscribe 按钮，点击后弹出订阅弹窗。

### 具体步骤

1.  **创建订阅弹窗组件 (`components/SubscribeOverlay.tsx`)**
    *   基于现有的 `SearchOverlay.tsx` 进行改造。
    *   **UI 调整**：
        *   将标题从 "Search" 改为 "Subscribe"。
        *   输入框提示语改为 "Enter your email address..."。
        *   添加一个 "Join" 或 "Subscribe" 的确认按钮。
    *   **逻辑调整**：
        *   移除搜索相关的过滤逻辑。
        *   添加简单的邮箱输入验证（前端模拟）。
        *   提交后显示“订阅成功”的反馈状态。

2.  **修改导航栏 (`components/Navigation.tsx`)**
    *   **桌面端**：
        *   将按钮文字由 "SEARCH" 改为 "SUBSCRIBE"。
        *   移除搜索图标，替换为 `Mail` 或保持无图标（仅文字）。
    *   **移动端**：
        *   将菜单底部的 "Start Searching" 改为 "Subscribe to Newsletter"。
        *   图标替换为 `Mail` icon。
    *   **Props**：将 `onSearchClick` 重命名为 `onSubscribeClick`。

3.  **集成到主应用 (`App.tsx`)**
    *   将状态 `isSearchOpen` 重命名为 `isSubscribeOpen`。
    *   引入并替换为 `SubscribeOverlay` 组件。
    *   将新的状态控制函数传递给 `Navigation`。

4.  **清理代码**
    *   移除不再使用的搜索相关逻辑（如未使用的引用）。

准备就绪，请确认是否开始执行。
