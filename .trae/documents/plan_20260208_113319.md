将在第三个项目（"Typography as Voice"）中展示全套组件（文本、列表、注释、引用、分割线），并重点实现统一的单媒体排版。

**方案详情：**

1. **新增** **`VideoBlock`** **组件**：

   * 创建 `VideoBlock`，样式上严格对齐 `ImageBlock`（圆角、阴影、细微边框）。

   * **排版策略**：采用“单图/单视频”布局，宽度设置为 `w-full`，使其与正文容器（`max-w-2xl`）严格等宽，确保视觉上的垂直对齐和协调感，符合“视觉美学”要求。

2. **更新类型定义**：

   * 在 `types.ts` 中扩展支持 `video` 类型。

3. **更新演示数据（Project ID: 3）**：

   * 重写 "Typography as Voice" 的内容，使其成为一个“组件展示厅”。

   * **文本流**：包含 H2 标题、正文段落、列表、高亮注释。

   * **媒体流**：

     * **图片**：展示一张标准宽度的静态图。

     * **GIF**：展示一张动态 GIF（复用图片组件，因其本质相同）。

     * **视频**：展示一个内嵌视频，保持与图片一致的容器样式。

4. \*\*代码实现

